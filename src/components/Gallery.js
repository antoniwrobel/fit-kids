import React from "react"

import { wrapper } from "../styles/gallery.module.css"
import * as G from "../styled/Gallery/styles"

const Gallery = () => {
  return <G.Wrapper className={wrapper}>gallery</G.Wrapper>
}

export default Gallery
